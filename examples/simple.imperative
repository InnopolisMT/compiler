var flag : boolean is true

type Address is record
    var streetCode : integer
    var number : integer
end

routine test(x: integer, z: real): real

routine main() 
is
    print flag
end

routine test(x: integer, z: real): real
is
    var zalupa is 4
    for i in 1..10 reverse loop
        z := z + i
    end
    if z > 30 then
        return z
    else
        return 0
    end
end